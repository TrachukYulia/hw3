# Пример для проекта на Node.js
name: learn-github-actions2
run-name: ${{ github.actor }} 
on: [push]
jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - uses: hw3/nodemodules/actions/eslint@3
